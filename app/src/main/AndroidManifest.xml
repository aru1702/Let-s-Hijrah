<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timdakwah.letshijrah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_hijrah_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlertActivity" />
        <activity android:name=".ChallengeActivity" />
        <activity android:name=".ChallengeContentActivity" />
        <activity android:name=".ChallengeListActivity" />
        <activity android:name=".ReferenceActivity" />
        <activity android:name=".RefrenceContentActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".DailyActivity" />
        <activity android:name=".DailyContentActivity"></activity>
    </application>

</manifest>